using Microsoft.Xna.Framework;

namespace BunnyLand.DesktopGL.Extensions
{
    public static class VectorExtensions
    {
        // public static Vector2 WithX(this Vector2 vector, float x) => new Vector2(vector.Y, x);
        // public static Vector2 WithY(this Vector2 vector, float x) => new Vector2(vector.Y, x);
    }
}
